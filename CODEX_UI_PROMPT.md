# 给 Codex 的 UI 改造任务提示词

> 用法:把本文件内容完整粘贴给 Codex。

---

## 任务

把 PixelFlow 的**顶部工具栏**和**底部导入栏**改造成**圆角浮窗风格的可折叠独立控件**,与现有的左右设备栏/信息栏(FloatingPanel)风格统一。默认收起,满足条件时自动展开。

## 需求清单

1. **顶部栏(工具栏:全选/取消/排序/星级筛选/缩略图滑块/AI分析)与底部栏(目标文件夹/导入按钮/高级选项)都改为圆角浮窗卡片**,视觉风格对齐现有左右面板(圆角 rounded-2xl、边框、阴影、半透明底)
2. **可折叠**:两个控件各自有收起/展开按钮,动画平滑(宽度/高度过渡,参考 `src/panel.tsx` 的 FloatingPanel 实现)
3. **默认收起**,满足任一条件自动展开:
   - 有照片被选中(selectedPaths 非空)
   - 打开了包含照片的子文件夹(照片列表非空)
   条件不满足时自动收起
4. **与设备栏/信息栏一样是独立控件**:参照 `src/panel.tsx` 的 FloatingPanel 模式(可加 glass 毛玻璃支持,与现有"透明毛玻璃背景"设置联动)
5. **动画要有**:展开/收起、出现/消失都要有平滑过渡,不能突兀跳变
6. **动手前先查官方文档**:模板来自 shadcn sidebar-04(`npx shadcn@latest add sidebar-04`,来源 https://ui.shadcn.com/blocks/sidebar ),先看 shadcn 官方文档和 sidebar-04 的交互模式,理解其可折叠/动画机制后,用符合项目现状的方式实现——不要机械照搬 sidebar 组件(它是侧边栏布局,我们是横向工具条),吸收其动画与交互思路即可

## 项目背景(先读再动手)

- 先读:`HANDOFF.md`(运行方式/代码结构/关键坑)、`PROJECT_LOG.md`(协作约定:每个改动必须写日志)
- 当前代码状态:
  - 前端已拆分组件:`src/components/`(title-bar、exif-panel、photo-card、scroll-fade-zone 等,小写连字符命名)
  - `src/panel.tsx` = FloatingPanel(圆角浮窗 + 可折叠 + glass 毛玻璃属性)——新控件要复用它或保持同风格
  - `src/App.tsx` 是主组装文件,顶部工具栏和底部导入栏的 JSX 在 App 主组件里
  - 界面文案全部走 i18n(`src/i18n/zh.ts` / `en.ts`),**新增文案必须同时加中英两版翻译**
  - 图标一律用 IconPark(`@icon-park/react`),不要用 emoji 图标
  - 窗口已启用透明 + Mica 毛玻璃(`tauri.conf.json` transparent: true,设置里有"透明毛玻璃背景"开关)

## 硬性约束(违反即失败)

1. **功能零改变**:工具栏和导入栏的所有按钮、下拉、滑块、输入框、进度条行为必须与现状完全一致,只是外观和显隐方式变化
2. **不得影响其他功能**:照片网格、查看器、设备面板、右键菜单、键盘快捷键、导入流程全部保持现状;改动只发生在中央区的工具栏与底部栏
3. **i18n 完整**:任何新增 UI 文本(如收起/展开提示)必须同时加到 zh.ts 和 en.ts,用 `t()` 调用
4. **不要引入新依赖**:shadcn sidebar-04 只是参考模板,吸收交互思路即可,不需要安装新包(除非确实必要且说明理由)
5. **默认收起逻辑要稳**:
   - 打开有照片的文件夹 → 底部栏展开(可操作导入)
   - 选中照片 → 底部栏展开、工具栏显示选中状态
   - 清空选择/文件夹无照片 → 自动收起
   - 注意:收起/展开不能用 CSS 动画硬切换造成布局抖动,使用条件渲染 + transition,或参考 FloatingPanel 的宽度过渡方案
6. **分步实施**:先做底部导入栏,验证通过后再做顶部工具栏(两者独立提交,便于回退)

## 工作规范(必须遵守)

1. **先计划再动手**:把改造方案(新组件结构、显隐条件、动画实现)写清楚给用户确认
2. **先查文档再动手**:shadcn sidebar-04 官方文档、FloatingPanel 现有实现、React transition 方案,理解后再实现
3. **verification-before-completion**:每步完成必须实际运行 `npx tsc --noEmit`、`npx tauri dev` 启动应用人工验证,有输出证据才允许声称完成
4. **systematic-debugging**:遇到 bug 先查根因,禁止猜测性修改
5. **遇到没有思路的问题,先到开源社区找成熟方案**(GitHub issues / shadcn 仓库 / Stack Overflow),理解原理后再动手
6. **不格式化轰炸**:只改本次任务相关的 JSX/样式,不重构无关代码,不重排 import
7. 每个阶段独立提交,提交信息中文,格式如 `feat: 底部导入栏改为可折叠圆角浮窗`

## 验证清单(完成后逐项打勾)

- [ ] `npx tsc --noEmit` 零错误
- [ ] `npx tauri dev` 启动,人工验证:
  - 启动时顶部/底部栏收起,界面干净
  - 点开有照片的文件夹 → 底部栏展开(带动画)
  - 选中照片 → 底部栏/工具栏状态正确
  - 收起按钮可手动收起/展开,动画平滑
  - 工具栏排序/星级筛选/AI 分析、导入栏选目录/导入/高级选项全部功能正常
  - 深色/浅色主题、透明毛玻璃开关下视觉正常
  - 中英文切换后新文案正确
- [ ] 设备面板、信息面板、照片网格、查看器(双击打开/切换/缩放)、右键菜单无回归
- [ ] `PROJECT_LOG.md` 已追加本次改动日志
- [ ] 已提交(可分 2 个提交:底部栏、顶部栏)
